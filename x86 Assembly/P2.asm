;输出1000 以内同时满足如下条件的数：个位数与十位数之和除以10 所得的余数等于百位数字。
DATAS SEGMENT
    ARRAY DB 3 DUP(0);此处输入数据段代码  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    MOV CX,1000;此处输入代码段代码
LOP:    
	LEA DI,ARRAY
    MOV AX,1000
    SUB AX,CX
    MOV DL,10
    DIV DL
    MOV [DI],AH
    ADD DI,TYPE ARRAY
    XOR AH,AH
    DIV DL
    MOV [DI],AH
    ADD DI,TYPE ARRAY
    MOV [DI],AL
    MOV DL,ARRAY[0]
    ADD AH,DL
    CMP AL,AH
    JE PRINT
    LOOP LOP
    MOV AH,4CH
    INT 21H
PRINT:
	;PUSH CX
	MOV DL,ARRAY[2]
	ADD DL,30H
	MOV AH,02H
	INT 21H
	MOV DL,ARRAY[1]
	ADD DL,30H
	MOV AH,02H
	INT 21H
	MOV DL,ARRAY[0]
	ADD DL,30H
	MOV AH,02H
	INT 21H
	MOV DL,2CH
	MOV AH,02H
	INT 21H
	DEC CX
	JMP LOP
CODES ENDS
    END START


